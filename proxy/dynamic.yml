http:
  middlewares:
    redirect-to-https:
      redirectScheme:
        scheme: https

  routers:
    dashboard:
      rule: "Host(`stox.systems/proxy`)" # Optional: For Traefik Dashboard
      service: api@internal
      middlewares: [redirect-to-https]
      tls:
        certResolver: le
